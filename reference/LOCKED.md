# Do not modify without explicit instruction

These elements from `design-source.html` are LOCKED for V1:

1. **PICTOGRAMS object** — all 12 SVG strings. Port verbatim. Do not "clean up" or "optimize" the SVG.
2. **JOURNEY array** — the six step model names and labels are final.
3. **SPAR_PROMPTS object** — the six clipboard prompts are written copy. Do not edit.
4. **Why-module copy** — every headline, viz-card label, module-foot line, and citation is locked.
5. **Color tokens** — the three theme palettes (a / c / d) are exact. No "accessibility adjustments" to the hex values without asking.
6. **Fraunces variable-font settings** — opsz and SOFT axis values are tuned. Do not normalize.

If the build requires changing any of these, stop and ask.
